from units import unitAbbr
import test_unitPrefix, test_unitSystem

unitAbbr:
    ps  = pico.seconds
    ns  = nano.seconds
    mis = micro.seconds
    ms  = mili.seconds

    nm  = nano.meters
    um  = micro.meters
    mm  = mili.meters
    dm  = deci.meters
    cm  = centi.meters
    km  = kilo.meters